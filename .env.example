# Railway Environment Variables
# These are automatically set when you add a PostgreSQL plugin in Railway.
# You only need to set these manually:

# NEXTAUTH_SECRET - Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=

# NEXTAUTH_URL - Set to your Railway domain (e.g., https://your-app.up.railway.app)
NEXTAUTH_URL=

# DATABASE_URL - Automatically provided by Railway PostgreSQL plugin
# DATABASE_URL=
